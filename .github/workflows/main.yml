name: Hello-World

on: push

jobs:
  my-job:
    runs-on: windows-latest
    steps:
    - name: environment-variable
      run: Get-Variable -ValueOnly
    - name: Python
      run: python3 --version
    - name: my-step
      run: echo "Hello World"
    
