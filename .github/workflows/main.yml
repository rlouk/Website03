name: Hello-World

on: push

jobs:
  my-job:
    runs-on: windows-latest
    steps:
    - name: environment-variable
      run: set
    - name: Python
      run: python3 --version
    - name: my-step
      run: echo "Hello World"
    
